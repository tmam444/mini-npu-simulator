# NPU 핵심 개념 시각화 안내서

NPU(Neural Processing Unit)의 이해를 돕기 위해 텍스트로 설명드렸던 핵심 개념 4가지를 시각 자료(Infographic)와 함께 쉽게 정리했습니다.

---

## 1. 양자화 (Quantization)란?

컴퓨터가 기억할 수 있는 메모리는 한정되어 있습니다. AI 모델의 크기가 너무 커지면 연산이 느려지고 메모리도 부족해집니다. 이를 해결하기 위해 데이터를 "단순하게 압축"하는 기술이 양자화입니다.

![양자화 설명](./assets/quantization_explained_1786861051378.jpg)

* **FP32 (32-bit 부동소수점)**: 왼쪽 파동처럼 매우 부드럽고 정밀한 값을 가집니다. 정확도는 높지만 메모리를 많이 차지(4 bytes)하고 계산이 무겁습니다.
* **INT8 (8-bit 정수)**: 오른쪽 계단처럼 값을 단순한 정수(예: 97, 33)로 뭉뚱그려 표현합니다. 디테일(정밀도)은 살짝 떨어지지만, 메모리는 1/4(1 byte)로 줄어들고 속도는 최대 4배까지 빨라집니다.
* **결론**: NPU는 정밀도를 살짝 포기하는 대신(INT8 사용), **압도적인 속도와 효율성**을 얻습니다.

---

## 2. 시스톨릭 어레이 (Systolic Array)와 PE 배열

CPU가 한 명의 천재 수학자라면, NPU는 수천 명의 학생(PE)들이 줄을 지어 앉아 단순 계산을 분업하는 형태입니다.

![시스톨릭 어레이 구조](./assets/systolic_array_pe_1786861060611.jpg)

* **PE (Processing Element)**: 곱셈과 덧셈(MAC 연산) 딱 하나만 할 줄 아는 아주 작은 계산기입니다.
* **데이터의 파도**: 그림을 보면 위에서는 가중치(Weight/Filter)가 내려오고, 왼쪽에서는 입력(Input) 값이 들어옵니다. 데이터가 마구잡이로 들어오는 게 아니라, 심장 박동(Systolic)이 뛰듯 규칙적인 박자에 맞춰 파도처럼 흘러갑니다.
* 모든 PE가 동시에 곱하고 더하는 연산을 수행하므로, 6만 개의 PE가 있다면 한 순간에 6만 번의 계산을 끝낼 수 있습니다.

---

## 3. 가중치 재사용 (Weight Reuse)의 마법

행렬 계산에서 가장 시간이 오래 걸리는 작업은 "계산" 자체가 아니라, **"데이터를 메모리에서 꺼내오는 시간(Memory Read)"**입니다.

![가중치 재사용 개념](./assets/weight_reuse_1786861299232.jpg)

* **WITHOUT Reuse (CPU 방식)**: 계산할 때마다 메모리에서 가중치와 입력값을 계속 새로 가져옵니다. 길이 너무 막힙니다.
* **WITH Reuse (NPU 방식)**: NPU 내부의 각 PE에 가중치(w1, w2, w3, w4)를 **단 한 번만 로드하여 고정(FIXED)**시킵니다. (위 자물쇠 아이콘 참고)
* 그런 다음 입력 데이터(x1, x2, x3...)만 컨베이어 벨트처럼 슉슉 흘려보냅니다.
* 메모리에서 가중치를 매번 불러올 필요가 없어져, 엄청난 속도 향상과 전력 절감이 이루어집니다.

---

## 4. NPU 실제 활용 사례

NPU의 무시무시한 병렬 처리 능력은 우리 실생활 곳곳에서 이미 쓰이고 있습니다.

![NPU 활용 사례](./assets/npu_use_cases_1786861763558.jpg)

1. **스마트폰 카메라**: 사진을 찍는 찰나의 25ms 동안, NPU는 노이즈 감소, HDR(밝기 조절), 얼굴 인식, 배경 날림(심도) 등 수십 개의 필터(행렬) 연산을 동시에 처리해냅니다.
2. **음성 인식 (Siri, 빅스비)**: 사용자의 목소리 파형을 2D 스펙트로그램(그리드 형태)으로 변환한 뒤, NPU가 순식간에 텍스트("안녕하세요")로 번역합니다. 
3. **LLM (ChatGPT 등)**: 단어 하나(토큰)를 만들어낼 때마다 수조 번의 행렬 곱셈이 일어납니다. NPU/TPU가 없다면 한 글자가 나오는 데 몇 초씩 걸렸을 것입니다.
4. **자율 주행**: 자동차 주위에 달린 8대의 카메라 영상을 초당 수십 장씩 실시간으로 분석해야 합니다. 차선, 보행자, 다른 차량을 0.1초 안에 감지하기 위해 초당 100조 번의 NPU 연산이 사용됩니다.
